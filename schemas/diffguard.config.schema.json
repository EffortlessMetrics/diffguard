{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ConfigFile",
  "description": "The on-disk configuration file.",
  "type": "object",
  "properties": {
    "defaults": {
      "default": {
        "base": "origin/main",
        "diff_context": 0,
        "fail_on": "error",
        "head": "HEAD",
        "max_findings": 200,
        "scope": "added"
      },
      "allOf": [
        {
          "$ref": "#/definitions/Defaults"
        }
      ]
    },
    "includes": {
      "description": "Include other config files. Paths are relative to this config file's directory. Rules are merged: later definitions override earlier ones by rule ID.",
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "rule": {
      "default": [],
      "type": "array",
      "items": {
        "$ref": "#/definitions/RuleConfig"
      }
    }
  },
  "definitions": {
    "Defaults": {
      "type": "object",
      "properties": {
        "base": {
          "type": [
            "string",
            "null"
          ]
        },
        "diff_context": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0.0
        },
        "fail_on": {
          "anyOf": [
            {
              "$ref": "#/definitions/FailOn"
            },
            {
              "type": "null"
            }
          ]
        },
        "head": {
          "type": [
            "string",
            "null"
          ]
        },
        "max_findings": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0.0
        },
        "scope": {
          "anyOf": [
            {
              "$ref": "#/definitions/Scope"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "FailOn": {
      "type": "string",
      "enum": [
        "error",
        "warn",
        "never"
      ]
    },
    "RuleConfig": {
      "type": "object",
      "required": [
        "id",
        "message",
        "patterns",
        "severity"
      ],
      "properties": {
        "exclude_paths": {
          "description": "Exclude path globs.",
          "default": [],
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "help": {
          "description": "Optional help text explaining how to fix violations.",
          "type": [
            "string",
            "null"
          ]
        },
        "id": {
          "type": "string"
        },
        "ignore_comments": {
          "default": false,
          "type": "boolean"
        },
        "ignore_strings": {
          "default": false,
          "type": "boolean"
        },
        "languages": {
          "description": "Optional language tags (e.g. \"rust\"). Empty means \"all\".",
          "default": [],
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "message": {
          "type": "string"
        },
        "paths": {
          "description": "Include path globs. Empty means \"all\".",
          "default": [],
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "patterns": {
          "description": "One or more regex patterns.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "severity": {
          "$ref": "#/definitions/Severity"
        },
        "tags": {
          "description": "Tags for grouping/filtering rules (e.g., \"debug\", \"security\", \"style\").",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "test_cases": {
          "description": "Test cases for validating this rule.",
          "type": "array",
          "items": {
            "$ref": "#/definitions/RuleTestCase"
          }
        },
        "url": {
          "description": "Optional URL with more information about the rule.",
          "type": [
            "string",
            "null"
          ]
        }
      }
    },
    "RuleTestCase": {
      "description": "A test case for validating a rule's behavior.",
      "type": "object",
      "required": [
        "input",
        "should_match"
      ],
      "properties": {
        "description": {
          "description": "Optional: description of what this test case validates.",
          "type": [
            "string",
            "null"
          ]
        },
        "ignore_comments": {
          "description": "Optional: override ignore_comments for this test case.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "ignore_strings": {
          "description": "Optional: override ignore_strings for this test case.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "input": {
          "description": "The input line to test against the rule.",
          "type": "string"
        },
        "language": {
          "description": "Optional: specify a language for preprocessing (e.g., \"rust\", \"python\").",
          "type": [
            "string",
            "null"
          ]
        },
        "should_match": {
          "description": "Whether the rule should match this input.",
          "type": "boolean"
        }
      }
    },
    "Scope": {
      "type": "string",
      "enum": [
        "added",
        "changed"
      ]
    },
    "Severity": {
      "type": "string",
      "enum": [
        "info",
        "warn",
        "error"
      ]
    }
  }
}