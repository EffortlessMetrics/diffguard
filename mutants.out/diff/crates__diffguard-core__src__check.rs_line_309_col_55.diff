--- crates/diffguard-core/src/check.rs
+++ replace > with == in compute_exit_code
@@ -301,17 +301,17 @@
     if matches!(fail_on, FailOn::Never) {
         return 0;
     }
 
     if counts.error > 0 {
         return 2;
     }
 
-    if matches!(fail_on, FailOn::Warn) && counts.warn > 0 {
+    if matches!(fail_on, FailOn::Warn) && counts.warn == /* ~ changed by cargo-mutants ~ */ 0 {
         return 3;
     }
 
     0
 }
 
 fn render_annotations(findings: &[Finding]) -> Vec<String> {
     findings
