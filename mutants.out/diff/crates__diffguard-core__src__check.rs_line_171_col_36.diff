--- crates/diffguard-core/src/check.rs
+++ replace > with == in run_check
@@ -163,17 +163,17 @@
             }
             continue;
         }
         filtered_findings.push(finding);
     }
 
     let verdict_status = if adjusted_counts.error > 0 {
         VerdictStatus::Fail
-    } else if adjusted_counts.warn > 0 {
+    } else if adjusted_counts.warn == /* ~ changed by cargo-mutants ~ */ 0 {
         VerdictStatus::Warn
     } else {
         VerdictStatus::Pass
     };
 
     let mut reasons: Vec<String> = Vec::new();
     if evaluation.truncated_findings > 0 {
         reasons.push(REASON_TRUNCATED.to_string());
