--- crates/diffguard-core/src/render.rs
+++ replace escape_md -> String with String::new()
@@ -82,17 +82,17 @@
         rule = escape_md(&f.rule_id),
         loc = loc,
         msg = msg,
         snippet = snippet
     )
 }
 
 fn escape_md(s: &str) -> String {
-    s.replace('|', "\\|").replace('`', "\\`")
+    String::new() /* ~ changed by cargo-mutants ~ */
 }
 
 #[cfg(test)]
 mod tests {
     use super::*;
 
     #[test]
     fn renders_markdown_table() {
