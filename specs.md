# Specification — work-ef8fdc94

## Feature/Behavior Description

Update the `RustQuality` preset documentation strings in `crates/diffguard/src/presets.rs` to use full namespaced rule IDs (`rust.no_unwrap`, `rust.no_dbg`, `rust.no_todo`, `rust.no_println`) that match the actual TOML generated by `generate_rust_quality()`, replacing the shorthand names (`no_unwrap`, `no_dbg`, `no_todo`, `no_print`) that don't match any actual rule ID.

## Acceptance Criteria

### AC-1: Doc comment uses namespaced rule IDs
**Given** the `RustQuality` enum variant at line 13 of `presets.rs`  
**When** the doc comment is read  
**Then** it reads: `/// Rust best practices (rust.no_unwrap, rust.no_dbg, rust.no_todo, rust.no_println)`

### AC-2: description() uses namespaced rule IDs
**Given** `Preset::RustQuality`  
**When** `description()` is called  
**Then** it returns: `"Rust best practices (rust.no_unwrap, rust.no_dbg, rust.no_todo, rust.no_println)"`

### AC-3: Existing tests pass
**Given** the code changes are applied  
**When** `cargo test -p diffguard` is run  
**Then** all tests pass (specifically `test_rust_quality_preset_generates_valid_toml` and `test_preset_descriptions`)

### AC-4: No behavioral change
**Given** `Preset::RustQuality::generate()` is called  
**When** the TOML output is examined  
**Then** it contains exactly 6 rules: `rust.no_unwrap`, `rust.no_expect`, `rust.no_dbg`, `rust.no_println`, `rust.no_todo`, `rust.no_unimplemented` (unchanged)

## Non-Goals

- This fix does NOT address the 6-vs-4 documentation gap (rust.no_expect and rust.no_unimplemented are not listed in the doc comment/description). That is a separate issue.
- This fix does NOT change any rule generation logic — only documentation strings are modified.
- This fix does NOT add new tests — existing tests don't verify specific string content and don't need updating.

## Dependencies

- No external dependencies
- No code logic changes required
- No schema changes required
- No coordination with other work items needed