# Plan: Close CLI Flag PRs #46 and #47

## Goal

Convert DRAFT PRs #46 (--color flag) and #47 (--version flag) to ready state, get them merged, and complete the v0.2 CLI enhancements.

## Current Context

- **PR #46** (DRAFT): `feat(cli): add --color flag` — adds `--color <never|always|auto>` using clap ColorChoice, wired to tracing_subscriber fmt layer. Work item: work-09e5782b.
- **PR #47** (DRAFT): `feat(cli): add --version flag` — adds `#[command(version)]` to Cli struct so clap auto-derives --version from CARGO_PKG_VERSION. Work item: work-3b090538.
- Both PRs are on branch `feat/v0.2-enhancements-v2`
- The `feat/v0.2-enhancements-v2` branch changes are reflected in modified `crates/diffguard/src/main.rs` in the working directory (on `main`)

## Proposed Approach

### Step 1: Checkout feat/v0.2-enhancements-v2 and inspect

```bash
git checkout feat/v0.2-enhancements-v2
git log --oneline feat/v0.2-enhancements-v2 ^main
```

### Step 2: Verify the changes

```bash
# Verify --color flag works
cargo run -- --color never --help | grep -A2 color
cargo run -- --color always --help | grep -A2 color

# Verify --version flag works
cargo run -- --version

# Run full test suite
cargo test --workspace
cargo clippy --workspace
```

### Step 3: Apply stash fix (ENV_LOCK race condition)

The stash contains `fix: add ENV_LOCK to cmd_validate tests to prevent race condition`. Apply it:
```bash
git stash pop
git log --oneline -1
```

### Step 4: Convert PRs to ready and merge

PRs are DRAFT — convert to ready and merge via GitHub UI or:
```bash
gh pr edit 46 --ready
gh pr edit 47 --ready
# Then merge (may need manual approval if CI not green)
```

### Step 5: After merge, close issue #46 and #47

Both issues are implied by the PRs. Add `Closes #46` / `Closes #47` to PR body when merging.

## Files Likely to Change

- `crates/diffguard/src/main.rs` — --color and --version implementation
- `crates/diffguard/tests/` — ENV_LOCK fix in xtask tests

## Risks / Tradeoffs

- PRs are DRAFT — need maintainer review before merge
- Stash application may have conflicts if feat/v0.2-enhancements-v2 diverged
- Both changes are small and low-risk

## Open Questions

- Who approves these PRs?
- Does CI need any adjustments for the feat branch?
