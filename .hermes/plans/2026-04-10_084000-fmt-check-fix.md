# Plan: Fix CI fmt Check Failure

## Goal
Fix `cargo fmt --check` failure blocking full CI compliance on branch `feat/work-8cb6a554/diffguard`.

## Current Context
`cargo xtask ci` fails at the `cargo fmt --check` gate with 3 formatting issues in `crates/diffguard-domain/tests/properties.rs`:
1. `property_missing_patterns_always_has_no_source` — `rule_ids` array should be multiline
2. `property_invalid_regex_source_downcasts_to_regex_error` — missing blank line before test
3. `property_invalid_regex_source_downcasts_to_regex_error` — `error.source().expect(...)` should be split across lines

These were likely introduced after the last fmt pass and are blocking CI.

## Proposed Approach
1. Run `cargo fmt` to auto-fix the formatting
2. Commit the formatting fix
3. Re-run `cargo xtask ci` to verify full CI passes

## Step-by-Step Plan
1. `cd ~/repos/diffguard && cargo fmt` — auto-apply formatting
2. `cd ~/repos/diffguard && cargo fmt --check` — verify clean
3. `cd ~/repos/diffguard && git diff --stat` — confirm only properties.rs changed
4. `cd ~/repos/diffguard && git add -A && git commit -m "style: apply fmt to diffguard-domain properties tests""`
5. `cd ~/repos/diffguard && cargo xtask ci` — verify full CI passes

## Files Likely to Change
- `crates/diffguard-domain/tests/properties.rs`

## Tests / Validation
- `cargo fmt --check` passes (0 diff)
- `cargo test --workspace` still passes
- `cargo xtask ci` completes without error

## Risks
- **Low**: Format-only change, no logic affected

## Effort
- **S** (small, <15 minutes)
