# Diffguard v0.2.0 Release & Branch Cleanup Plan

**Date:** 2026-04-06  
**Branch:** chore/hermes-index  
**Goal:** Ship v0.2.0 to crates.io and clean up outstanding branches.

---

## Current State

- **Build:** ✅ Clean (cargo clippy, 0 warnings)
- **Tests:** ✅ 913+ tests passing across all branches
- **PR #5:** ✅ Merged (v0.2 enhancements — LSP, multi-base, analytics)
- **Open Issues:** None
- **Open PRs:** None
- **ROADMAP:** Phases 1–9 all marked complete
- **CHANGELOG:** Has `[Unreleased]` section with per-directory overrides, fuzz target, scope expansion

## Unmerged Branches (all tests passing)

| Branch | Purpose | Status |
|--------|---------|--------|
| `chore/hermes-index` | Agent context consolidation + crates.io publish prep | Current branch, ready |
| `fix/config-includes-and-defaults` | Config include DAG + defaults merge fix | Ready to merge |
| `fix/lsp-git-diff-timeout` | LSP git diff timeout + ENV_LOCK race | Ready to merge |
| `fix/xtask-binary-path` | xtask binary path resolution + mutex poison | Ready to merge |

## Prioritized Work

### P0: Merge fix branches into main
1. Merge `fix/xtask-binary-path` (narrow scope, xtask only)
2. Merge `fix/lsp-git-diff-timeout` (LSP + conformance fixes)
3. Merge `fix/config-includes-and-defaults` (config system fix)
4. Each can be a separate PR or consolidated into one "post-v0.2 bugfixes" PR

### P1: Merge `chore/hermes-index` into main
- Consolidates agent context into `.hermes/agent-context.md`
- Adds publish script and release notes
- Cleans up Cargo.toml metadata for crates.io

### P2: Decide on unreleased CHANGELOG items
The `[Unreleased]` section has:
- Per-directory rule overrides via `.diffguard.toml`
- `evaluate_lines` fuzz target
- Scope expansion (`scope = "deleted"`, `scope = "modified"`)

These are already in the codebase (merged via PR #5). Options:
- **Option A:** Release as v0.2.1 patch (update version, CHANGELOG date)
- **Option B:** Bump to v0.3.0 if semver warrants it
- **Option C:** Fold into v0.2.0 if not yet published to crates.io

### P3: Publish to crates.io
- `scripts/publish.sh` exists but is not executable (`chmod +x` needed)
- Publish order: types → diff → domain → core → analytics → main → lsp
- Requires `cargo login` to be set up

### P4: Post-release cleanup
- Delete merged fix branches
- Tag v0.2.0 release on GitHub
- Update README badges if needed

## Verification Steps

1. `cargo test --workspace` on each branch before merge
2. `cargo clippy --workspace` — zero warnings
3. `cargo publish --dry-run -p diffguard-types` — verify metadata
4. After publish: `cargo install diffguard && diffguard --version`

## Risks

- **crates.io publish order matters** — dependencies must be published first (script handles this)
- **30s delay between publishes** — needed for crates.io indexing
- **`rust-version = "1.92"`** — verify this is available on target systems
