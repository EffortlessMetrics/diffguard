--- crates/diffguard-domain/src/evaluate.rs
+++ replace < with <= in find_multiline_matches
@@ -396,17 +396,17 @@
     out
 }
 
 fn find_multiline_matches(
     rule: &CompiledRule,
     file_indices: &[usize],
     prepared_lines: &[PreparedLine],
 ) -> Vec<RawMatchEvent> {
-    if file_indices.len() < 2 {
+    if file_indices.len() <= /* ~ changed by cargo-mutants ~ */ 2 {
         return Vec::new();
     }
 
     let mut seen = BTreeSet::<(usize, usize, String)>::new();
     let mut out = Vec::new();
 
     for start in 0..file_indices.len() {
         let end = (start + rule.multiline_window).min(file_indices.len());
