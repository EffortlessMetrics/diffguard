--- crates/diffguard-diff/src/unified.rs
+++ delete match arm b'n' in unescape_git_path
@@ -517,17 +517,17 @@
         let Some(next) = iter.next() else {
             out.push(b'\\');
             break;
         };
 
         match next {
             b'\\' => out.push(b'\\'),
             b'"' => out.push(b'"'),
-            b'n' => out.push(b'\n'),
+             /* ~ changed by cargo-mutants ~ */
             b't' => out.push(b'\t'),
             b'r' => out.push(b'\r'),
             b' ' => out.push(b' '),
             b'0'..=b'7' => {
                 let mut val = (next - b'0') as u32;
                 for _ in 0..2 {
                     match iter.peek().copied() {
                         Some(d) if (b'0'..=b'7').contains(&d) => {
