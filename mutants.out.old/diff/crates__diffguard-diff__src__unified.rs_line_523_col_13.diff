--- crates/diffguard-diff/src/unified.rs
+++ delete match arm b'\\' in unescape_git_path
@@ -515,17 +515,17 @@
         }
 
         let Some(next) = iter.next() else {
             out.push(b'\\');
             break;
         };
 
         match next {
-            b'\\' => out.push(b'\\'),
+             /* ~ changed by cargo-mutants ~ */
             b'"' => out.push(b'"'),
             b'n' => out.push(b'\n'),
             b't' => out.push(b'\t'),
             b'r' => out.push(b'\r'),
             b' ' => out.push(b' '),
             b'0'..=b'7' => {
                 let mut val = (next - b'0') as u32;
                 for _ in 0..2 {
