--- crates/diffguard-diff/src/unified.rs
+++ replace && with || in tokenize_git_paths
@@ -466,17 +466,17 @@
             in_quote = true;
             quoted = true;
             continue;
         }
 
         buf.push(ch);
     }
 
-    if !buf.is_empty() && tokens.len() < limit {
+    if !buf.is_empty() || /* ~ changed by cargo-mutants ~ */ tokens.len() < limit {
         tokens.push(GitPathToken { value: buf, quoted });
     }
 
     tokens
 }
 
 fn parse_single_git_path(input: &str) -> Option<String> {
     let tokens = tokenize_git_paths(input, 1);
